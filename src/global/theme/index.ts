// Decidir qual tema usar

import { Appearance } from "react-native";
import light from "./light";

// const theme = Appearance.getColorScheme() === "light" ? light : dark
const theme = light;

export default theme;
